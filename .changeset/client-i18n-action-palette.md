---
"@jmfederico/pi-web": patch
---

Localize the action palette. Action text is translated where the registry assembles it, so core actions, bundled plugin actions, and third-party ones all pass through one seam; text with no catalog entry is left exactly as the plugin wrote it.
